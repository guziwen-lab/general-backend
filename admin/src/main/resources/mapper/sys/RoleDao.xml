<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.supermap.modules.sys.dao.RoleDao">
    <resultMap type="com.supermap.modules.sys.entity.RoleEntity" id="roleMap">
        <result property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getRoleNamesByUserId" resultType="java.lang.String">
        select distinct role.role_name
        from sys_role role
                 left join sys_user_role_relation r on role.role_id = r.role_id
        where r.user_id = #{userId}
    </select>

    <select id="getRoleByUserId" resultMap="roleMap">
        select distinct role.*
        from sys_role role
                 left join sys_user_role_relation r on role.role_id = r.role_id
        where r.user_id = #{userId}
    </select>
</mapper>